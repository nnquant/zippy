[package]
name = "zippy-io"
version.workspace = true
edition.workspace = true

[features]
default = []
zmq-publisher = ["dep:zmq"]

[dependencies]
arrow.workspace = true
parquet.workspace = true
zippy-core = { path = "../zippy-core" }
zmq = { version = "0.10.0", optional = true }
